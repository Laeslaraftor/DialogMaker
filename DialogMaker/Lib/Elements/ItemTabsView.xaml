<UserControl x:Class="DialogMaker.Lib.Elements.ItemTabsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DialogMaker.Lib.Elements"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <TabControl x:Name="_tabControl">
        <TabControl.ItemTemplate>
            <DataTemplate>
                <local:CloseView Title="{Binding Name, Mode=TwoWay}"
                                 CanClose="{Binding CanClose}"
                                 CanEditTitle="{Binding CanRename}"
                                 CloseCommandParameter="{Binding}"
                                 Click="OnCloseViewClicked"/>
            </DataTemplate>
        </TabControl.ItemTemplate>
        <TabControl.ContentTemplate>
            <DataTemplate>
                <ContentPresenter Content="{Binding TabContent}"/>
            </DataTemplate>
        </TabControl.ContentTemplate>
    </TabControl>
</UserControl>
