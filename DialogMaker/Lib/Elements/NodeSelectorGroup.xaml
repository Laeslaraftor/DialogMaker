<UserControl x:Class="DialogMaker.Lib.Elements.NodeSelectorGroup"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DialogMaker.Lib.Elements"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border BorderBrush="{DynamicResource DividerStrokeColorDefaultBrush}"
            BorderThickness="1" CornerRadius="4" Margin="0,1">
        <Grid x:Name="_mainGrid">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Button FontFamily="{StaticResource SymbolThemeFontFamily}"
                    VerticalAlignment="Center"
                    FontSize="12" Padding="2" Margin="2,0,4,0"
                    Click="OnMinimizeButtonClicked">
                <TextBlock Text="&#xE76C;" RenderTransformOrigin="0.5,0.5">
                    <TextBlock.RenderTransform>
                        <RotateTransform x:Name="_minimizeButtonRotation"/>
                    </TextBlock.RenderTransform>
                </TextBlock>
            </Button>
            <TextBlock Grid.Column="1" Text="{Binding Name}" 
                       FontSize="16" FontWeight="Medium" Margin="0,2,4,2"/>
            <ListView x:Name="_itemsList" Grid.Row="1" Grid.ColumnSpan="2"
                      Visibility="Collapsed"
                      ItemsSource="{Binding Children}"
                      Style="{StaticResource NoSelectionListView}"
                      ScrollViewer.VerticalScrollBarVisibility="Disabled">
                <ListView.Resources>
                    <DataTemplate x:Key="_containerTemplate">
                        <local:NodeSelectorGroup RootItem="{Binding}"
                                                 ItemSelected="OnChildNodeSelectorGroupItemSelected">
                            <local:NodeSelectorGroup.Style>
                                <Style TargetType="local:NodeSelectorGroup">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsEnabled}" Value="True">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsEnabled}" Value="False">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </local:NodeSelectorGroup.Style>
                        </local:NodeSelectorGroup>
                    </DataTemplate>
                    <DataTemplate x:Key="_itemTemplate">
                        <Border Margin="0,1" CornerRadius="2"
                                Cursor="Hand">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource ControlStrokeColorDefaultBrush}"/>
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="False">
                                            <Setter Property="Background" Value="{DynamicResource ControlStrokeColorOnAccentSecondaryBrush}"/>
                                        </Trigger>
                                        <DataTrigger Binding="{Binding IsEnabled}" Value="True">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsEnabled}" Value="False">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <Grid Margin="4">
                                <Border BorderBrush="{DynamicResource FocusStrokeColorOuterBrush}"
                                        BorderThickness="2" CornerRadius="4" Margin="-4">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding IsSelected}" Value="False">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <Border BorderBrush="{DynamicResource FocusStrokeColorInnerBrush}"
                                            BorderThickness="2" CornerRadius="2"/>
                                </Border>
                                <Grid Margin="4,2">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock FontFamily="{StaticResource SymbolThemeFontFamily}" 
                                               Text="&#xE710;" FontSize="12" Margin="0,0,4,0"
                                               VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="1" Text="{Binding Name}"/>
                                </Grid>
                                <Button Grid.ColumnSpan="2" Click="OnSelectButtonClicked" Opacity="0"
                                        HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.Resources>
                <ListView.Template>
                    <ControlTemplate>
                        <ItemsPresenter/>
                    </ControlTemplate>
                </ListView.Template>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ContentPresenter ContentStringFormat="{}{0}">
                            <ContentPresenter.Style>
                                <Style TargetType="ContentPresenter">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsContainer}" Value="True">
                                            <Setter Property="ContentTemplate" Value="{StaticResource _containerTemplate}"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsContainer}" Value="False">
                                            <Setter Property="ContentTemplate" Value="{StaticResource _itemTemplate}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ContentPresenter.Style>
                        </ContentPresenter>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </Border>
</UserControl>
