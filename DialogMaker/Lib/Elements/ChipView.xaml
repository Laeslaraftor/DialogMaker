<UserControl x:Class="DialogMaker.Lib.Elements.ChipView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DialogMaker.Lib.Elements"
             mc:Ignorable="d" x:Name="_main"
             d:DesignHeight="450" d:DesignWidth="800"
             IsEnabledChanged="OnMainIsEnabledChanged">
    <UserControl.Style>
        <Style TargetType="UserControl">
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.5"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="True">
                    <Setter Property="Opacity" Value="1"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Style>
    <Grid>
        <Border x:Name="_mainBorder" CornerRadius="4"
                BorderThickness="2"
                BorderBrush="{DynamicResource AccentFillColorDefaultBrush}">
            <Grid>
                <Border x:Name="_backgroundBorder" Opacity="0"
                        Background="{Binding ElementName=_mainBorder, Path=BorderBrush}"
                        SizeChanged="OnBackgroundBorderSizeChanged">
                    <TextBlock FontWeight="Medium" Margin="6,2" TextAlignment="Center"
                               Text="{Binding ElementName=_text, Path=Text}"
                               Foreground="{DynamicResource TextOnAccentFillColorPrimaryBrush}"/>
                </Border>
                <TextBlock x:Name="_text" FontWeight="Medium" Margin="6,2" TextAlignment="Center"
                           Foreground="{Binding ElementName=_mainBorder, Path=BorderBrush}"/>
                <Canvas>
                    <Canvas.Style>
                        <Style TargetType="Canvas">
                            <Style.Triggers>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="True">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Canvas.Style>
                    <Line x:Name="_diagonalLine" X1="0" Y2="0"
                          Stroke="{Binding ElementName=_mainBorder, Path=BorderBrush}"
                          StrokeThickness="2"/>
                </Canvas>
            </Grid>
        </Border>
        <Button Click="OnButtonClicked" Opacity="0" Padding="0" 
                HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
    </Grid>
</UserControl>
