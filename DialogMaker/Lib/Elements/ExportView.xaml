<UserControl x:Class="DialogMaker.Lib.Elements.ExportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DialogMaker.Lib.Elements"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="4">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <local:Entry x:Name="_outputPathEntry" Placeholder="Путь к папке выхода"
                         ConfirmedText="OnPathEntryConfirmedText"/>
            <Button Grid.Column="1" Content="&#xE838;" Padding="8"
                    FontFamily="{StaticResource SymbolThemeFontFamily}"
                    Click="OnSelectFolderButtonClicked" FontSize="16"
                    Margin="4,0" ToolTip="Выбрать папку"/>
            <Button Grid.Column="2" Content="&#xE8DA;" Padding="8"
                    FontFamily="{StaticResource SymbolThemeFontFamily}"
                    Click="OnOpenSelectedFolderButtonClicked" FontSize="16"
                    ToolTip="Открыть выбранную папку"/>
            <local:ToolTipView x:Name="_pathErrorView" Grid.Row="1" Grid.ColumnSpan="3"
                               Text="Неверный путь к папке экспорта" 
                               MessageType="Error" Visibility="Collapsed"
                               Margin="0,4"/>
        </Grid>
        <ListView x:Name="_outputList" Grid.Row="1" MinHeight="200" 
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock VerticalAlignment="Center"
                                   TextWrapping="Wrap">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsError}" Value="True">
                                            <Setter Property="Foreground" Value="{DynamicResource SystemFillColorCriticalBrush}"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsError}" Value="False">
                                            <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                            <Run Text="{Binding Prefix}"/>
                            <Run Text="{Binding Item}"/>
                        </TextBlock>
                        <Grid Grid.Column="1">
                            <Grid.Style>
                                <Style TargetType="Grid">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding NotStarted}" Value="True">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding NotStarted}" Value="False">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Grid.Style>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <ProgressBar Width="150" Value="{Binding Progress}"/>
                            <Label Grid.Column="2" Margin="8,0,0,0" Padding="0"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   FontSize="18" FontWeight="Medium"
                                   Content="{Binding Progress}" ContentStringFormat=" {0}%"/>
                        </Grid>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="_exportButton" 
                    Content="Экспортировать"
                    HorizontalAlignment="Center"
                    Click="OnExportButtonClicked"
                    ToolTip="Экспортировать проект в выбранную папку"/>
            <ProgressBar x:Name="_exportProgressBar" Grid.Column="1"
                         Margin="8,0,0,0"/>
            <TextBlock x:Name="_exportProgressPercent" Grid.Column="2"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       FontSize="18" FontWeight="Medium"
                       Margin="8,0"/>
        </Grid>
    </Grid>
</UserControl>
