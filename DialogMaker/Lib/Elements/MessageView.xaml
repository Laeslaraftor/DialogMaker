<UserControl x:Class="DialogMaker.Lib.Elements.MessageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DialogMaker.Lib.Elements"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border Background="{DynamicResource LayerOnMicaBaseAltFillColorDefaultBrush}"
            CornerRadius="4">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="30"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <StackPanel Orientation="Horizontal">
                <TextBlock x:Name="_dateTime" FontSize="12"
                           VerticalAlignment="Center"
                           Opacity="0.6" Margin="8,0,0,0"/>
                <local:ToolTipView x:Name="_messageImportance" Visibility="Collapsed"
                                   VerticalAlignment="Center" Margin="2,2,0,0"
                                   RenderTransformOrigin="0.5,0.5">
                    <local:ToolTipView.RenderTransform>
                        <ScaleTransform ScaleX="0.75" ScaleY="0.75"/>
                    </local:ToolTipView.RenderTransform>
                </local:ToolTipView>
            </StackPanel>
            <local:ChipView x:Name="_readStatus" Text="Новое" 
                            Grid.RowSpan="2" VerticalAlignment="Top"
                            Margin="8,5,8,0" FontSize="12"
                            HorizontalAlignment="Right"/>
            <TextBlock Grid.Row="1" x:Name="_title" 
                       FontSize="20" FontWeight="Medium" Margin="8,0,8,8"
                       TextWrapping="Wrap"/>
            <TextBlock Grid.Row="2" x:Name="_text"
                       FontSize="16" Margin="8,0,8,8" TextWrapping="Wrap"/>
            <ListView x:Name="_commandsList" Grid.Row="3" Margin="8,0,8,8"
                      Style="{StaticResource NoSelectionListView}">
                <ListView.Template>
                    <ControlTemplate>
                        <ItemsPresenter/>
                    </ControlTemplate>
                </ListView.Template>
                <ListView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ListView.ItemsPanel>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Button Content="{Binding Name}"
                                ToolTip="{Binding Description}"
                                Command="{Binding}" Margin="2"/>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </Border>
</UserControl>
