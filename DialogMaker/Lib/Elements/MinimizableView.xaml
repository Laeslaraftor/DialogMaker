<UserControl x:Class="DialogMaker.Lib.Elements.MinimizableView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DialogMaker.Lib.Elements"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Button FontFamily="{StaticResource SymbolThemeFontFamily}" 
                Content="&#xE70D;" VerticalAlignment="Center"
                Padding="6" Click="OnMinimizeButtonClicked"
                RenderTransformOrigin="0.5,0.5">
            <Button.RenderTransform>
                <RotateTransform x:Name="_minimizeButtonRotation"/>
            </Button.RenderTransform>
        </Button>
        <Border x:Name="_heightBorder"
                Grid.Row="1"
                Width="2" Background="Gray" CornerRadius="2"
                HorizontalAlignment="Center" Margin="0,6"/>
        <Border x:Name="_headerContainer"
                Grid.Column="1"/>
        <Border x:Name="_contentContainer"
                Grid.Column="1" Grid.Row="1"/>
    </Grid>
</UserControl>
