<UserControl x:Class="DialogMaker.Lib.Elements.DialogCompilerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DialogMaker.Lib.Elements"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <TabControl Height="800">
        <TabItem Header="Предпросмотр">
            <ListView x:Name="_sectionView">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid IsHitTestVisible="False">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>
                            <TextBlock FontSize="18" FontWeight="Medium">
                                <Run Text="Сегмент "/>
                                <Run Text="{Binding Index, Mode=OneWay}"/>
                            </TextBlock>
                            <ListView Grid.Row="1" ItemsSource="{Binding Operations}">
                                <ListView.ItemContainerStyle>
                                    <Style TargetType="ListViewItem"/>
                                </ListView.ItemContainerStyle>
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <Label Content="{Binding}" Padding="0"/>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </TabItem>
        <TabItem Header="Код">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="300"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <local:DialogPlayer x:Name="_dialogPlayer"/>
                <ScrollViewer HorizontalScrollBarVisibility="Disabled"
                              VerticalScrollBarVisibility="Auto"
                              Grid.Column="1">
                    <ListView x:Name="_codeView"
                              ScrollViewer.VerticalScrollBarVisibility="Disabled">
                        <ListView.Template>
                            <ControlTemplate>
                                <ItemsPresenter/>
                            </ControlTemplate>
                        </ListView.Template>
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid IsHitTestVisible="False">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition/>
                                    </Grid.RowDefinitions>
                                    <TextBlock FontSize="18" FontWeight="Medium">
                                        <Run Text="Сегмент "/>
                                        <Run Text="{Binding Index, Mode=OneWay}"/>
                                    </TextBlock>
                                    <ListView Grid.Row="1" ItemsSource="{Binding}">
                                        <ListView.ItemContainerStyle>
                                            <Style TargetType="ListViewItem"/>
                                        </ListView.ItemContainerStyle>
                                        <ListView.ItemTemplate>
                                            <DataTemplate>
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition/>
                                                        <ColumnDefinition/>
                                                    </Grid.ColumnDefinitions>
                                                    <Label Content="{Binding Key}"/>
                                                    <Label Grid.Column="1" ContentStringFormat=": {0}" Content="{Binding Value}" Padding="0"/>
                                                </Grid>
                                            </DataTemplate>
                                        </ListView.ItemTemplate>
                                    </ListView>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </ScrollViewer>
                <ListView x:Name="_resourcesList"
                              Grid.Column="2">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{DynamicResource LayerFillColorDefaultBrush}"
                                    BorderBrush="{DynamicResource SurfaceStrokeColorDefaultBrush}"
                                    CornerRadius="4" BorderThickness="1" Padding="4">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition/>
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition />
                                    </Grid.ColumnDefinitions>
                                    <Label ContentStringFormat="№{0}" Content="{Binding Index, Mode=OneWay}"
                                           FontWeight="Medium" FontSize="20" VerticalAlignment="Center"
                                           Grid.RowSpan="2" Margin="0,0,4,0"/>
                                    <StackPanel Orientation="Horizontal" Grid.Column="1">
                                        <local:EnumValueView Value="{Binding Reference.Type}"
                                                             HorizontalAlignment="Left"/>
                                        <local:ChipView Text="{Binding ResourceItem.Id}"/>
                                    </StackPanel>
                                    <Label Grid.Row="1" Grid.Column="1" MaxWidth="300"
                                           Content="{Binding ResourceItem}" HorizontalAlignment="Left"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </TabItem>
    </TabControl>
</UserControl>
