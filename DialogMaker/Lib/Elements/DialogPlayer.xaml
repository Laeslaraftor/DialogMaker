<UserControl x:Class="DialogMaker.Lib.Elements.DialogPlayer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DialogMaker.Lib.Elements"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <local:DialogViewer x:Name="_viewer"/>
        <Grid Grid.Row="1" Margin="0,4">
            <Button x:Name="_controlButton"
                    HorizontalAlignment="Left"
                    FontFamily="{StaticResource SymbolThemeFontFamily}"
                    Click="OnControlButtonClick"/>
            <Button HorizontalAlignment="Right"
                    FontFamily="{StaticResource SymbolThemeFontFamily}"
                    Click="OnClearButtonClicked">
                <TextBlock Foreground="{DynamicResource SystemFillColorCriticalBrush}"
                           Text="&#xE74D;"/>
            </Button>
        </Grid>
    </Grid>
</UserControl>
