<UserControl x:Class="DialogMaker.Lib.Elements.MediaControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DialogMaker.Lib.Elements"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <local:ToolTipView x:Name="_errorView" Visibility="Collapsed"
                           MessageType="Error" Margin="0,0,0,4"/>
        <MediaElement x:Name="_media" Grid.Row="1"
                      MediaOpened="OnMediaOpened"
                      MediaFailed="OnMediaFailed"
                      MediaEnded="OnMediaEnded"
                      LoadedBehavior="Manual"
                      UnloadedBehavior="Manual"
                      ScrubbingEnabled="True" Grid.RowSpan="2"/>
        <local:PlayerView x:Name="_controls" Grid.Row="2"
                          PositionChanged="OnControlsPositionChanged"
                          ControlButtonClicked="OnControlsControlButtonClicked"
                          VolumeChanged="OnControlsVolumeChanged"/>
    </Grid>
</UserControl>
